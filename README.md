# Запуск

```
docker-compose up
docker exec -it {container php} bash
composer install
```

создать базу данных task, запустить скрипт для создания таблицы task (sql скрипт находиться в папке script)

открыть http://localhost:8002